LEEME
____________

Este directorio contiene los archivos para sacar una funcion a un fichero de script
y luego importarlo.

Si el script es de tipo module <script type='module'> deben ser de tipo module el 
script que contiene a ls funcion funcion.js y la que contiene el resto del codigo
javascript.

El script funcion.js que contiene las funciones a incluir debe ir aztes del codigo del 
script que va incluido en el main.html ya que javascript igual que CSS son lenguajes 
interpretados e influye si va aztes o luegp en el codigo.

En el main.html debemos imoprtar las funciones con import y entre llaves los nombre de 
las funciones a importar.

En funcion.js iran incluidas las funciones y se añadira un export aztes de la definicion de
la funcion:  export function mostrarSegundoCombo(). Los nombres de la funciones siguen el 
formato CamelCase.

